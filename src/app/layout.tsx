import './globals.css'; import type {Metadata} from 'next'; export const metadata:Metadata={title:'Invoza — Beautiful invoices. Less admin.',description:'Create professional invoices in under 60 seconds. No signup required, no watermark, free PDF download.',metadataBase:new URL('http://localhost:3000'),openGraph:{title:'Invoza — Beautiful invoices. Less admin.',description:'Create beautiful invoices and download polished PDFs without complicated accounting software.',type:'website'}}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
