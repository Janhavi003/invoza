import {describe,it,expect} from 'vitest'; import {calculateInvoice} from '@/lib/calculations';
const base:any={items:[{id:'1',description:'x',quantity:'2',rate:'100',taxRate:'0'}],discount:'0',taxKind:'none',taxRate:'0',gstMode:'none',amountPaid:'0'};
describe('invoice calculations',()=>{it('subtotal',()=>expect(calculateInvoice(base).subtotal).toBe('200.00'));it('discount and tax',()=>expect(calculateInvoice({...base,discount:'20',taxKind:'custom',taxRate:'10'})).toMatchObject({discount:'20.00',tax:'18.00',total:'198.00'}));it('decimal quantities',()=>expect(calculateInvoice({...base,items:[{...base.items[0],quantity:'1.25',rate:'80'}]}).subtotal).toBe('100.00'));it('multiple items',()=>expect(calculateInvoice({...base,items:[...base.items,{id:'2',description:'y',quantity:'3',rate:'50',taxRate:'0'}]}).subtotal).toBe('350.00'));it('zero tax',()=>expect(calculateInvoice({...base,taxKind:'gst',taxRate:'0'}).tax).toBe('0.00'));it('paid balance',()=>expect(calculateInvoice({...base,amountPaid:'50'}).balanceDue).toBe('150.00'));it('large numbers',()=>expect(calculateInvoice({...base,items:[{...base.items[0],quantity:'1000000',rate:'999999.99'}]}).total).toBe('999999990000.00'));it('negative inputs are safe',()=>expect(calculateInvoice({...base,discount:'-50',amountPaid:'-2'})).toMatchObject({discount:'0.00',amountPaid:'0.00'}));it('per-line tax',()=>expect(calculateInvoice({...base,items:[{...base.items[0],taxRate:'18'}]}).tax).toBe('36.00'));it('GST-style combined rate',()=>expect(calculateInvoice({...base,taxKind:'gst',taxRate:'18',gstMode:'cgst_sgst'}).tax).toBe('36.00'));});
